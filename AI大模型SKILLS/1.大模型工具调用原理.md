
# 大模型工具调用原理

## 1.以ChatGPT为例

### 调用流程


```plantuml
@startuml
title ChatGPT Layered Architecture

top to bottom direction
skinparam shadowing false
skinparam defaultTextAlignment center
skinparam ArrowThickness 1.2
skinparam linetype ortho
skinparam packageStyle rectangle
skinparam rectangle {
  RoundCorner 12
  BorderColor #6B7280
  FontColor #111827
  BackgroundColor #F9FAFB
}

rectangle "交互层 / Client Layer\nWeb / Mobile / Desktop / API Clients" as Client #E8F0FE
rectangle "接入层 / Access Layer\nAPI Gateway / Auth / Rate Limit / Session Mgmt" as Access #EAF7EA
rectangle "编排层 / Orchestration Layer\nPrompt Assembly / Context Mgmt / Tool Routing / Workflow\nPlanning / Retry / Streaming / Response Merge" as Orchestration #FFF4E5

together {
  rectangle "模型层 / Model Layer\nLLM Inference\nModel Routing" as Model #F3E8FF
  rectangle "工具层 / Tool Layer\nSearch / Browser\nCode / Weather" as Tool #FCE7F3
  rectangle "检索层 / Retrieval Layer\nEmbedding\nVector DB / RAG" as Retrieval #E0F2FE
}

rectangle "数据层 / Data Layer\nConversation Store / User Profile / Cache / Logs / Metrics" as Data #F3F4F6
rectangle "安全治理层 / Safety & Governance\nModeration / Policy / Access Control / Audit / Redaction" as Safety #FEE2E2

Client --> Access
Access --> Orchestration

Orchestration --> Model
Orchestration --> Tool
Orchestration --> Retrieval

Model --> Data
Tool --> Data
Retrieval --> Data

Data --> Safety

@enduml

```


```plantuml
@startuml
title ChatGPT 高层时序图（近似真实架构）

actor User as U
participant "ChatGPT Client\n(Web/iOS/Android)" as C
participant "API Gateway / Edge" as G
participant "Conversation Service\n(session, auth, rate limit)" as S
participant "Orchestrator / Agent Runtime\n(tool routing, policy, planning)" as O
participant "LLM Inference Service" as M
participant "Tool Executor" as E
database "Conversation Store\n(history, metadata)" as D
collections "Retrieval / Search Index" as R
participant "External Tools\n(Search, Weather, Calculator,\nCode Runner, Connectors)" as T
participant "Safety / Policy Service" as P

U -> C: 输入问题
C -> G: HTTPS请求
G -> S: 转发请求
S -> D: 读取会话历史/用户配置
D --> S: 返回历史与元数据

S -> P: 预检查(权限/风控/内容策略)
P --> S: 允许/拦截/降级策略

S -> O: 创建本轮执行上下文
O -> M: 发送消息\n(system prompt + history + tool schemas + policy)

alt 模型可直接回答
    M --> O: 直接文本回复
else 模型决定调用工具
    M --> O: tool call(s)\n例如 search_web(query=...)
    O -> E: 执行工具调用
    E -> T: 调用外部工具/API
    T --> E: 返回结构化结果
    E --> O: 工具结果

    opt 需要检索/搜索融合
        O -> R: 查询索引/搜索结果融合
        R --> O: 返回检索片段
    end

    O -> M: 工具结果 + 检索片段 + 上下文
    M --> O: 生成最终答案
end

O -> P: 后检查(安全/合规/脱敏)
P --> O: 通过/重写/拦截

O -> D: 写入对话历史、工具调用记录、元数据
D --> O: OK

O --> S: 返回最终结果
S --> G: 响应
G --> C: 流式/非流式返回答案
C --> U: 展示答案

@enduml

```

```plantuml
@startuml
title ChatGPT 流式输出时序图（近似真实架构）

actor User as U
participant "Client" as C
participant "Gateway" as G
participant "Conversation Service" as S
participant "Orchestrator" as O
participant "LLM Inference" as M
participant "Tool Executor" as E
participant "External Tool" as T

U -> C: 发送消息
C -> G: 请求
G -> S: 转发
S -> O: 建立执行上下文
O -> M: 请求生成

alt 直接回答
    loop token streaming
        M --> O: token chunk
        O --> S: chunk
        S --> G: chunk
        G --> C: SSE/WebSocket chunk
        C --> U: 增量展示文本
    end
else 需要工具
    M --> O: tool call
    O -> E: 执行工具
    E -> T: 调用外部API
    T --> E: 返回结果
    E --> O: 工具结果
    O -> M: 继续生成最终回答

    loop token streaming
        M --> O: token chunk
        O --> S: chunk
        S --> G: chunk
        G --> C: SSE/WebSocket chunk
        C --> U: 增量展示文本
    end
end

@enduml

```



### 客户端层
就是你看到的 ChatGPT 产品形态：
- Web
- 手机 App
- 桌面端

它们只负责：
- 收集用户输入
- 展示模型输出
- 处理流式显示、附件上传、按钮交互等

---

### 平台/边缘层
这一层通常包括：
- API Gateway
- 鉴权
- 限流
- 风控
- 会话服务

作用是把“用户请求”变成“一个可执行的 AI 任务”。

---

### Orchestrator / Agent Runtime
这层是关键中的关键。  
如果说 LLM 是大脑，这层就是**调度中枢**。

负责：
- 拼装 prompt
- 决定给模型哪些工具可用
- 接收模型返回的 tool call
- 调用工具执行
- 把工具结果再次送回模型
- 控制多轮调用次数
- 处理失败重试
- 记录日志与轨迹

这也是为什么说，**真正的 ChatGPT 不只是一个模型，而是一个“模型 + 调度系统 + 工具系统 + 安全系统”的整体产品。**

---

### 模型层
这里不只是一个模型实例，通常还会有：
- 模型路由（不同任务走不同模型）
- 推理服务
- embedding 服务（做检索）

例如：
- 简单问答走快速模型
- 复杂推理走更强模型
- 检索时调用 embedding 模型

---

### 数据与知识层
包括：
- 对话历史
- 用户偏好
- 检索索引
- 日志审计

模型本身不是“数据库”，真正的产品必须把这些数据放在外部系统里。

---

### 工具层
包括：
- 搜索
- 浏览器
- 代码执行沙箱
- 计算器
- 天气
- 企业连接器

模型只能“决定调用”，真正执行在工具层。

---

### 安全治理层
真实产品一定会有这层，不然风险非常大。

例如：
- 敏感内容审核
- PII 脱敏
- 权限检查
- 工具访问控制
- 输出合规检查

---
