
# 大模型工具调用原理

在 Agentic AI 的开发实践中，大模型的能力扩展依赖三种核心机制。RAG（检索增强生成）解决的是"如何获取外部知识"，Tool Calling / Function Calling 解决的是"如何执行外部操作"，而 Skills（声明式技能）解决的是更高层的问题——如何将一组 Prompt 指令、Tool 绑定和可选的 RAG 配置打包为一个可复用的能力单元，并在运行时按需加载。

Skills 的核心价值是"渐进式上下文披露"（Progressive Disclosure）：不在对话开始时就把所有工具描述和指令塞进 Prompt，而是根据用户意图语义匹配最相关的 Skill，按需加载完整指令和关联工具。这样既节省 token 消耗，又让 Agent 能管理大量技能而不会因为上下文过长而降低效果。

## 1.以ChatGPT为例

### 1.1.调用流程

![协作关系图](https://github.com/clsaa/ai-notes/releases/download/plantuml-assets/1_layered_architecture.png)


```plantuml
@startuml
title ChatGPT Layered Architecture

top to bottom direction
skinparam shadowing false
skinparam defaultTextAlignment center
skinparam ArrowThickness 1.2
skinparam linetype ortho
skinparam packageStyle rectangle
skinparam rectangle {
  RoundCorner 12
  BorderColor #6B7280
  FontColor #111827
  BackgroundColor #F9FAFB
}

rectangle "交互层 / Client Layer\nWeb / Mobile / Desktop / API Clients" as Client #E8F0FE
rectangle "接入层 / Access Layer\nAPI Gateway / Auth / Rate Limit / Session Mgmt" as Access #EAF7EA
rectangle "编排层 / Orchestration Layer\nPrompt Assembly / Context Mgmt / Tool Routing / Workflow\nPlanning / Retry / Streaming / Response Merge" as Orchestration #FFF4E5

together {
  rectangle "模型层 / Model Layer\nLLM Inference\nModel Routing" as Model #F3E8FF
  rectangle "工具层 / Tool Layer\nSearch / Browser\nCode / Weather" as Tool #FCE7F3
  rectangle "检索层 / Retrieval Layer\nEmbedding\nVector DB / RAG" as Retrieval #E0F2FE
}

rectangle "数据层 / Data Layer\nConversation Store / User Profile / Cache / Logs / Metrics" as Data #F3F4F6
rectangle "安全治理层 / Safety & Governance\nModeration / Policy / Access Control / Audit / Redaction" as Safety #FEE2E2

Client --> Access
Access --> Orchestration

Orchestration --> Model
Orchestration --> Tool
Orchestration --> Retrieval

Model --> Data
Tool --> Data
Retrieval --> Data

Data --> Safety

@enduml

```

![协作关系图](https://github.com/clsaa/ai-notes/releases/download/plantuml-assets/2_high_level_sequence.png)


```plantuml
@startuml
title ChatGPT 高层时序图（近似真实架构）

actor User as U
participant "ChatGPT Client\n(Web/iOS/Android)" as C
participant "API Gateway / Edge" as G
participant "Conversation Service\n(session, auth, rate limit)" as S
participant "Orchestrator / Agent Runtime\n(tool routing, policy, planning)" as O
participant "LLM Inference Service" as M
participant "Tool Executor" as E
database "Conversation Store\n(history, metadata)" as D
collections "Retrieval / Search Index" as R
participant "External Tools\n(Search, Weather, Calculator,\nCode Runner, Connectors)" as T
participant "Safety / Policy Service" as P

U -> C: 输入问题
C -> G: HTTPS请求
G -> S: 转发请求
S -> D: 读取会话历史/用户配置
D --> S: 返回历史与元数据

S -> P: 预检查(权限/风控/内容策略)
P --> S: 允许/拦截/降级策略

S -> O: 创建本轮执行上下文
O -> M: 发送消息\n(system prompt + history + tool schemas + policy)

alt 模型可直接回答
    M --> O: 直接文本回复
else 模型决定调用工具
    M --> O: tool call(s)\n例如 search_web(query=...)
    O -> E: 执行工具调用
    E -> T: 调用外部工具/API
    T --> E: 返回结构化结果
    E --> O: 工具结果

    opt 需要检索/搜索融合
        O -> R: 查询索引/搜索结果融合
        R --> O: 返回检索片段
    end

    O -> M: 工具结果 + 检索片段 + 上下文
    M --> O: 生成最终答案
end

O -> P: 后检查(安全/合规/脱敏)
P --> O: 通过/重写/拦截

O -> D: 写入对话历史、工具调用记录、元数据
D --> O: OK

O --> S: 返回最终结果
S --> G: 响应
G --> C: 流式/非流式返回答案
C --> U: 展示答案

@enduml

```

![协作关系图](https://github.com/clsaa/ai-notes/releases/download/plantuml-assets/3_streaming_sequence.png)

```plantuml
@startuml
title ChatGPT 流式输出时序图（近似真实架构）

actor User as U
participant "Client" as C
participant "Gateway" as G
participant "Conversation Service" as S
participant "Orchestrator" as O
participant "LLM Inference" as M
participant "Tool Executor" as E
participant "External Tool" as T

U -> C: 发送消息
C -> G: 请求
G -> S: 转发
S -> O: 建立执行上下文
O -> M: 请求生成

alt 直接回答
    loop token streaming
        M --> O: token chunk
        O --> S: chunk
        S --> G: chunk
        G --> C: SSE/WebSocket chunk
        C --> U: 增量展示文本
    end
else 需要工具
    M --> O: tool call
    O -> E: 执行工具
    E -> T: 调用外部API
    T --> E: 返回结果
    E --> O: 工具结果
    O -> M: 继续生成最终回答

    loop token streaming
        M --> O: token chunk
        O --> S: chunk
        S --> G: chunk
        G --> C: SSE/WebSocket chunk
        C --> U: 增量展示文本
    end
end

@enduml

```



### 1.2.客户端层
就是你看到的 ChatGPT 产品形态：
- Web
- 手机 App
- 桌面端

它们只负责：
- 收集用户输入
- 展示模型输出
- 处理流式显示、附件上传、按钮交互等

---

### 1.3.平台/边缘层
这一层通常包括：
- API Gateway
- 鉴权
- 限流
- 风控
- 会话服务

作用是把“用户请求”变成“一个可执行的 AI 任务”。

---

### 1.4.Orchestrator / Agent Runtime
这层是关键中的关键。  
如果说 LLM 是大脑，这层就是**调度中枢**。

负责：
- 拼装 prompt
- 决定给模型哪些工具可用
- 接收模型返回的 tool call
- 调用工具执行
- 把工具结果再次送回模型
- 控制多轮调用次数
- 处理失败重试
- 记录日志与轨迹

这也是为什么说，**真正的 ChatGPT 不只是一个模型，而是一个“模型 + 调度系统 + 工具系统 + 安全系统”的整体产品。**

---

### 1.5.模型层
这里不只是一个模型实例，通常还会有：
- 模型路由（不同任务走不同模型）
- 推理服务
- embedding 服务（做检索）

例如：
- 简单问答走快速模型
- 复杂推理走更强模型
- 检索时调用 embedding 模型

---

### 1.6.数据与知识层
包括：
- 对话历史
- 用户偏好
- 检索索引
- 日志审计

模型本身不是“数据库”，真正的产品必须把这些数据放在外部系统里。

---

### 1.7.工具层
包括：
- 搜索
- 浏览器
- 代码执行沙箱
- 计算器
- 天气
- 企业连接器

模型只能“决定调用”，真正执行在工具层。

---

### 1.8.安全治理层
真实产品一定会有这层，不然风险非常大。

例如：
- 敏感内容审核
- PII 脱敏
- 权限检查
- 工具访问控制
- 输出合规检查

---



## 2 Spring AI 对SKILLS的支持

Spring AI 1.0 GA（2025 年 5 月发布）内置了两类核心扩展机制。Tool Calling 通过 `@Tool` 注解声明工具方法，在对话时由模型自主决策是否调用，框架屏蔽 OpenAI、Anthropic、Google 等不同提供商的协议差异。MCP 协议集成遵循 Model Context Protocol 标准，连接外部服务和数据源。

然而，Spring AI 官方核心目前没有内置声明式 Skills 管理模块——没有 SKILL.md 文件解析、没有技能注册中心、没有按需激活机制。

### 2.1 Spring AI Alibaba 的 Agent Skills 框架

阿里巴巴的社区扩展项目 Spring AI Alibaba 在 1.1.2.0 版本中引入了 Agent Skills 框架。其核心设计：每个 Skill 是一个目录，包含一个 SKILL.md 文件，采用 YAML Front Matter + Markdown Body 的结构。启动时框架只解析元数据摘要，运行时通过内置的 `read_skill` 工具按需加载完整指令。这种格式源自微软提出的 Agent Skills 开放规范。


| 维度 | Tool Calling | MCP | Skills |
|------|-------------|-----|--------|
| 抽象层次 | 基础设施原语 | 跨平台协议 | 应用层模式 |
| 加载方式 | 启动时全量注入 | 运行时按协议发现 | 运行时按需匹配加载 |
| 定义方式 | `@Tool` 注解 / 代码 | JSON-RPC 协议 | SKILL.md 声明文件 |
| 上下文占用 | 所有工具描述常驻 Prompt | 按协议动态获取 | 仅匹配命中的 Skill 注入 |
| Spring AI 支持 | 官方核心内置 | 官方核心内置 | 仅 Spring AI Alibaba 扩展 |


### 2.2.为什么Spring AI没有Skills

这不是疏忽，而是一个有意的架构决策，背后有三层原因。

**抽象层次不同。** RAG 涉及一整套底层技术组件（Embedding Model、Vector Store、文档解析与分块、检索策略），不同向量数据库的 API 差异巨大，框架必须提供统一抽象。Tool Calling 需要屏蔽各模型提供商的 function calling 协议差异。这两者本质上是"基础设施适配"问题——如果框架不做，开发者很难自己做好。而 Skills 不引入任何新的底层技术组件，它的每个组成部分（Prompt 模板、Tool 注册、动态上下文管理）都已经被 Spring AI 现有原语（ChatClient、@Tool、Advisor）覆盖。换句话说，Skills 如果框架不做，开发者用现有积木就能搭出来。

**Spring 的设计哲学。** Spring 一贯提供正交的、可组合的基础构建块，而不是过早地固化上层应用模式。正如 Spring Framework 提供了 IoC 和 AOP 原语，但并没有内置 CQRS 或 Saga 模式。Skills 作为一种 Agent 组织模式，同样属于应用层选择。

**规范成熟度。** 微软提出的 Agent Skills 开放格式还在早期阶段，社区对"Skill 应该长什么样"尚无共识。Spring 团队倾向于等标准成熟后再纳入核心。

### 2.3 如何设计和实现SpringAI-Skills

#### 模块边界

在进入具体设计之前，首先必须明确 Skills 模块"该做什么"和"不该做什么"。这是整个设计最关键的决策——边界一旦模糊，模块就会膨胀为一个"万能 Agent 平台"，与 Tool Calling、RAG、Workflow 产生不必要的职责重叠。

Skills 模块的一句话定义：**负责把声明式 Markdown 技能资产转化为可参与 AI 调用上下文的结构化能力描述。**

它负责七件事：资产加载、文档解析、注册发现、语义匹配、注入策略、生命周期治理、可观测性。它不负责六件事：执行业务动作（由 Tool Calling / MCP 负责）、向量检索（由 RAG / VectorStore 负责）、复杂工作流编排（由 Agent Workflow 负责）、替代 PromptTemplate、控制模型调用参数、替代 MCP 协议。


![Skills模块边界定义](https://github.com/clsaa/ai-notes/releases/download/plantuml-assets/skills-boundary-definition.png)

```plantuml
@startuml skills-boundary-definition
!theme plain
skinparam backgroundColor #FEFEFE
skinparam defaultFontSize 12
skinparam componentStyle rectangle
skinparam linetype ortho
skinparam nodesep 40
skinparam ranksep 35

title Skills 模块边界定义 —— 该做什么 vs 不该做什么

package "Skills 模块职责 (DO)" as do_pkg #E8F5E9 {
    [资产加载\nSkillLoader / SkillSource\n从 classpath / 文件系统 / Git 加载] as do1
    [文档解析\nSkillParser\nMarkdown + YAML Front Matter] as do2
    [注册发现\nSkillRegistry\n按 name / tag / scope / version 查询] as do3
    [语义匹配\nSkillResolver\nEmbedding / Keyword / LLM / Composite] as do4
    [注入策略\nSkillInjectionPolicy\n全量 / 分段 / 约束 / 引用 / 工具绑定] as do5
    [生命周期治理\n版本 / 启禁用 / 优先级 / 作用域 / 热更新] as do6
    [可观测性\nSkillUsageRecorder\n记录命中率 / 耗时 / 审计] as do7
}

package "不属于 Skills 的职责 (DON'T)" as dont_pkg #FFEBEE {
    [执行业务动作\n→ 由 Tool Calling / MCP 负责] as dont1
    [向量检索\n→ 由 RAG / VectorStore 负责] as dont2
    [复杂工作流编排\n→ 由 Agent Workflow 负责] as dont3
    [替代 PromptTemplate\n→ Skill 是更高层资产] as dont4
    [控制模型调用参数\n→ 由 ChatModel 层负责] as dont5
    [替代 MCP 协议\n→ Skill 可引用 MCP 工具但不实现协议] as dont6
}

note bottom of do_pkg
    **一句话定义：**
    Skills 模块负责把声明式 Markdown 技能资产
    转化为可参与 AI 调用上下文的
    结构化能力描述。
end note

note bottom of dont_pkg
    **设计红线：**
    Skills 是声明和约束层
    不是执行能力层
    不是编排引擎
end note

@enduml
```

#### 模块生命周期


从应用启动（自动配置、扫描解析、Embedding 预计算）到运行时按需懒加载，再到文件变化触发热更新的完整生命周期：


![Skill完整生命周期](https://github.com/clsaa/ai-notes/releases/download/plantuml-assets/spring-ai-skills-seq-lifecycle.png)

```plantuml
@startuml spring-ai-skills-seq-lifecycle
!theme plain
skinparam backgroundColor #FEFEFE
skinparam defaultFontSize 12
skinparam sequenceMessageAlign center
skinparam maxMessageSize 180
skinparam responseMessageBelowArrow true

title Skill 完整生命周期 —— 从发现到热更新

participant "Spring Boot\nApplication" as app
participant "SkillAuto\nConfiguration" as autoconf #FFE0B2
participant "SkillProperties" as props #FFE0B2
participant "SkillSource\n(Composite)" as source #FFE0B2
participant "SkillParser" as parser #FFE0B2
participant "SkillRegistry" as registry #FFE0B2
participant "EmbeddingModel" as emb
participant "FileSystem\nWatchService" as watch
database "SKILL.md\nFiles" as files

== Phase 1: 启动 - 自动配置 ==

app -> autoconf : @EnableAutoConfiguration
activate autoconf

autoconf -> props : 读取 spring.ai.skills.*
activate props
note right of props
    spring.ai.skills:
      locations:
        - classpath:skills/
        - file:/opt/skills/
      resolve-strategy: composite
      max-active-skills: 3
      lazy-load: true
      hot-reload: true
end note
props --> autoconf : 配置项
deactivate props

autoconf -> source : 创建 CompositeSkillSource
activate source
autoconf -> registry : 创建 InMemorySkillRegistry
activate registry

== Phase 2: 发现 - Skill 扫描与解析 ==

registry -> source : discover()
source -> files : 扫描所有 locations
activate files
files --> source : SKILL.md 文件列表
deactivate files

loop 每个 SKILL.md
    source -> parser : parseMetadataOnly(resource)
    activate parser
    parser -> parser : 解析 YAML Front Matter
    note right of parser
        仅读取头部元数据
        name / description / tags / tools
        跳过 Markdown Body (懒加载)
    end note
    parser --> source : Skill (摘要, instructions=null)
    deactivate parser
end

source --> registry : List<Skill> (摘要集合)

== Phase 3: 索引 - Embedding 预计算 ==

registry -> emb : 批量计算所有 Skill\ndescription 的 Embedding
activate emb
note right of emb
    N 个 Skill 描述
    → N 个向量
    缓存在内存中
    用于运行时快速匹配
end note
emb --> registry : Map<skillName, float[]>
deactivate emb

note over registry
    Registry 就绪
    N 个 Skill 摘要 + Embedding 缓存
    等待运行时请求
end note

deactivate registry
deactivate source

participant "SkillActivator\n(Caller)" as caller

== Phase 4: 运行时 - 按需加载 (懒加载) ==

caller -> registry : activate("code-review")
activate registry
registry -> registry : skill.isLoaded() == false
registry -> files : 读取完整 SKILL.md
activate files
files --> registry : 完整内容 (含 instructions)
deactivate files
registry -> parser : parseMarkdownBody(content)
activate parser
parser --> registry : instructions 正文
deactivate parser
registry -> registry : skill.setInstructions(body)\nskill.setLoaded(true)
note over registry : 后续请求直接使用缓存\n无需再次读取文件
registry --> caller : Skill (完整)
deactivate registry

== Phase 5: 热更新 - 文件变化监听 ==

autoconf -> watch : 注册 WatchService\n(仅 file:// 路径)
activate watch

note over watch
    监听 /opt/skills/ 目录
    CREATE / MODIFY / DELETE 事件
end note

files -> watch : MODIFY: code-review/SKILL.md
activate files
deactivate files

watch -> registry : onSkillModified("code-review")
activate registry
registry -> parser : 重新解析 SKILL.md
activate parser
parser --> registry : 更新后的 Skill
deactivate parser
registry -> emb : 重新计算 Embedding
activate emb
emb --> registry : 新向量
deactivate emb
registry -> registry : 替换缓存中的 Skill\n标记 loaded=false (下次懒加载)
note over registry : 热更新完成\n无需重启应用
deactivate registry

files -> watch : CREATE: new-skill/SKILL.md
watch -> registry : onSkillCreated("new-skill")
activate registry
registry -> registry : 注册新 Skill
deactivate registry

files -> watch : DELETE: old-skill/SKILL.md
watch -> registry : onSkillDeleted("old-skill")
activate registry
registry -> registry : 注销 Skill
deactivate registry

deactivate watch
deactivate autoconf

@enduml
```


#### 核心模块

spring-ai-skills 内部分为六个子包，每个子包职责单一、边界清晰。

**core 包**定义数据模型。`Skill` 是顶层接口，`SkillDefinition` 是标准实现，`SkillMetadata` 承载 tags、version、author、priority、scope 等元信息，`SkillResource` 表示原始文件资源（路径、checksum、modifiedTime、来源仓库），`SkillContext` 是激活后的输出载体。

**discovery 包**负责 Skill 的发现与解析。`SkillSource` 策略接口提供三种实现：ClasspathSkillSource（类路径扫描）、FileSystemSkillSource（文件系统扫描，支持 WatchService 热更新）、CompositeSkillSource（组合多来源）。`SkillParser` 负责解析 SKILL.md，支持"仅解析元数据"的懒加载模式。

**registry 包**管理注册与查询。`SkillRegistry` 接口支持 register / unregister / get / findByTag / findByScope 等操作，InMemorySkillRegistry 是默认实现。

**resolution 包**实现语义匹配。`SkillResolver` 策略接口提供四种实现：EmbeddingSkillResolver（向量相似度）、KeywordSkillResolver（关键词/Tag 匹配）、LlmSkillResolver（让 LLM 判断选哪个 Skill）、CompositeSkillResolver（组合策略，支持 Pipeline / Vote / FirstMatch 三种模式）。SkillResolver 被设计为 SPI，框架提供默认实现但不做成"黑盒魔法"。

**activation 包**处理按需激活。`SkillActivator` 加载完整指令，解析关联工具，并通过 `SkillInjectionPolicy` 决定如何将 Skill 注入请求上下文。

**observability 包**记录使用指标。`SkillUsageRecorder` 追踪 Skill 命中率、耗时、每次请求激活了哪个 Skill，支持审计和优化。


![Spring AI Skills核心API类图](https://github.com/clsaa/ai-notes/releases/download/plantuml-assets/spring-ai-skills-core-class.png)

```plantuml
@startuml spring-ai-skills-core-class
!theme plain
skinparam backgroundColor #FEFEFE
skinparam defaultFontSize 12
skinparam linetype ortho
skinparam classAttributeIconSize 0

title Spring AI Skills - 核心 API 类图

package "skills-core" #FFF3E0 {

    interface Skill {
        + getName(): String
        + getDescription(): String
        + getMetadata(): SkillMetadata
        + getInstructions(): String
        + getToolNames(): List<String>
        + isLoaded(): boolean
    }

    class SkillDefinition implements Skill {
        - name: String
        - description: String
        - metadata: SkillMetadata
        - instructions: String
        - toolNames: List<String>
        - loaded: boolean
        --
        + {static} builder(): Builder
    }

    class SkillMetadata {
        - tags: List<String>
        - version: String
        - author: String
        - priority: int
        - properties: Map<String, Object>
    }

    class SkillContext {
        - activatedSkills: List<Skill>
        - enhancedSystemPrompt: String
        - dynamicToolCallbacks: List<ToolCallback>
        - metadata: Map<String, Object>
        --
        + merge(other: SkillContext): SkillContext
    }

    Skill "1" *-- "1" SkillMetadata
}

package "skills-discovery" #FFF3E0 {

    interface SkillSource {
        + discover(): List<Skill>
    }

    class ClasspathSkillSource implements SkillSource {
        - locations: List<String>
        - parser: SkillParser
    }

    class FileSystemSkillSource implements SkillSource {
        - paths: List<Path>
        - parser: SkillParser
        - watchService: WatchService
        + enableHotReload(): void
    }

    class CompositeSkillSource implements SkillSource {
        - sources: List<SkillSource>
    }

    class SkillParser {
        + parse(resource: Resource): Skill
        + parseMetadataOnly(resource: Resource): Skill
        - parseYamlFrontMatter(content: String): SkillMetadata
        - parseMarkdownBody(content: String): String
    }
}

package "skills-registry" #FFF3E0 {

    interface SkillRegistry {
        + register(skill: Skill): void
        + unregister(name: String): void
        + get(name: String): Optional<Skill>
        + findByTag(tag: String): List<Skill>
        + getAll(): List<Skill>
        + getAllSummaries(): List<Skill>
        + refresh(): void
    }

    class InMemorySkillRegistry implements SkillRegistry {
        - skills: ConcurrentHashMap<String, Skill>
        - skillSource: SkillSource
        --
        + {method} @PostConstruct init()
    }
}

package "skills-resolution" #FFF3E0 {

    interface SkillResolver {
        + resolve(request: SkillResolveRequest): List<Skill>
    }

    class SkillResolveRequest {
        - userMessage: String
        - conversationHistory: List<Message>
        - candidateSkills: List<Skill>
        - maxResults: int
        - threshold: double
    }

    class EmbeddingSkillResolver implements SkillResolver {
        - embeddingModel: EmbeddingModel
        - skillEmbeddingCache: Map<String, float[]>
        - similarityThreshold: double
    }

    class KeywordSkillResolver implements SkillResolver {
        - tokenizer: Function<String, List<String>>
    }

    class LlmSkillResolver implements SkillResolver {
        - chatModel: ChatModel
        - selectionPromptTemplate: String
    }

    class CompositeSkillResolver implements SkillResolver {
        - resolvers: List<SkillResolver>
        - strategy: CompositionStrategy
    }

    enum CompositionStrategy {
        PIPELINE
        VOTE
        FIRST_MATCH
    }
}

package "skills-activation" #FFF3E0 {

    class SkillActivator {
        - skillRegistry: SkillRegistry
        - toolCallbackProvider: ToolCallbackProvider
        --
        + activate(skills: List<Skill>,\n    originalPrompt: String): SkillContext
        - loadFullInstructions(skill: Skill): String
        - resolveToolCallbacks(toolNames: List<String>): List<ToolCallback>
        - buildEnhancedPrompt(original: String,\n    skills: List<Skill>): String
    }
}

package "spring-ai-advisors-skills" #FFE0B2 {

    class SkillAdvisor {
        - skillRegistry: SkillRegistry
        - skillResolver: SkillResolver
        - skillActivator: SkillActivator
        - maxActiveSkills: int
        --
        + before(request: AdvisedRequest): AdvisedRequest
        + after(response: AdvisedResponse): AdvisedResponse
        + getOrder(): int
        ..
        implements CallAroundAdvisor
    }
}

SkillAdvisor --> SkillRegistry : 查询
SkillAdvisor --> SkillResolver : 匹配
SkillAdvisor --> SkillActivator : 激活
SkillActivator --> SkillRegistry : 加载完整指令
SkillActivator ..> SkillContext : 产出
InMemorySkillRegistry --> SkillSource : 发现
SkillParser <.. ClasspathSkillSource : 使用
SkillParser <.. FileSystemSkillSource : 使用
SkillResolver ..> SkillResolveRequest : 输入

@enduml
```

#### 注入策略：SkillInjectionPolicy

这是设计中容易被忽视但极为关键的一层抽象。Skill 被匹配命中后，"如何注入到 LLM 调用上下文"本身就是一个需要策略化的独立关切。不同场景需要不同的注入方式，不能简单地"把 Markdown 原文塞进 System Prompt"。

SkillInjectionPolicy 定义为策略接口，提供五种实现：

**FullInstructionPolicy（全量注入）**：将 Skill 的全部 instructions 合并进 System Prompt。最简单直接，但 token 消耗最高。适合 Skill 内容较短、且需要模型完整理解指令的场景。

**StructuredBlockPolicy（分段注入）**：将 SKILL.md 中的 Purpose、Rules、Examples 等章节拆成独立的 context block 分别注入。模型可以更好地区分不同类型的指令。

**ConstraintOnlyPolicy（约束优先）**：只注入 Rules / Constraints 部分，不注入示例和完整说明。适合 token 敏感或安全合规场景。

**ReferenceSummaryPolicy（引用模式）**：只向模型提供 Skill 摘要列表和一个 `read_skill` 工具，模型按需调用工具加载完整内容。这是 Spring AI Alibaba 采用的方案，token 效率最高。

**ToolBindingPolicy（工具绑定模式）**：只根据 Skill 声明暴露相应工具，不注入任何文本指令。适合 Skill 的核心价值在于工具集而非指令文本的场景。


![SkillInjectionPolicy注入策略设计](https://github.com/clsaa/ai-notes/releases/download/plantuml-assets/skills-injection-policy.png)

```plantuml
@startuml skills-injection-policy
!theme plain
skinparam backgroundColor #FEFEFE
skinparam defaultFontSize 12
skinparam linetype ortho
skinparam classAttributeIconSize 0

title SkillInjectionPolicy — 注入策略设计

package "spring-ai-skills :: injection" #FFF3E0 {

    interface SkillInjectionPolicy {
        + inject(skill: Skill, request: AdvisedRequest): AdvisedRequest
        + getName(): String
    }

    class FullInstructionPolicy implements SkillInjectionPolicy {
        将 Skill 全部内容合并进
        System Prompt
    }

    class StructuredBlockPolicy implements SkillInjectionPolicy {
        将 Purpose / Rules / Examples
        拆成独立 block 分段注入
    }

    class ConstraintOnlyPolicy implements SkillInjectionPolicy {
        只注入约束规则
        不注入示例和完整说明
    }

    class ReferenceSummaryPolicy implements SkillInjectionPolicy {
        只注入 Skill 摘要列表
        模型按需通过 read_skill 工具
        加载完整内容
    }

    class ToolBindingPolicy implements SkillInjectionPolicy {
        只暴露 Skill 关联的工具
        不注入任何文本指令
    }

    class CompositeInjectionPolicy implements SkillInjectionPolicy {
        - policies: List<SkillInjectionPolicy>
        组合多种策略
    }
}

package "spring-ai-skills :: activation" #FFF3E0 {
    class SkillActivator {
        - injectionPolicy: SkillInjectionPolicy
        --
        根据配置选择注入策略
    }
}

SkillActivator --> SkillInjectionPolicy : 使用

note bottom of SkillInjectionPolicy
    配置方式：
    spring.ai.skills.injection-mode:
      full / structured / constraint-only
      / reference / tool-binding
end note

@enduml
```




#### 设计中应该避免的陷阱

**不要把 Skill 和 Tool 混成一个概念。** Skill 是声明和约束层，Tool 是执行能力层。Skill 可以引用 Tool，但不应该成为"执行器"。

**不要把 Skill 做成完整 Agent Workflow DSL。** 一旦 Skill 承担编排职责，模块范围就会失控。多步任务规划、多 Agent 协作属于 orchestration engine 的职责。

**不要把 Markdown 解析规则写得过于死板。** SkillParser 应该是可插拔的 SPI，允许自定义解析规则，不要把 Markdown 结构（必须有 # Purpose / # Rules 等章节）硬编码。

**不要让"自动选 Skill"成为黑盒魔法。** SkillResolver 应该是 SPI + 可观测的，提供多种策略供选择，而不是内置一个不透明的 AI 自动选择器。

**不要让 Skill 决定底层模型调用细节。** 模型参数（temperature、max_tokens 等）和调用方式仍是 ChatModel 层的职责。

**不要把 Skill 做成 PromptTemplate 的替代品。** Skill 是更高层的能力资产，底层的模板拼装仍由 PromptTemplate 体系处理。